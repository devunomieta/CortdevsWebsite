
  # Agency Multipage Website

  This is a code bundle for Agency Multipage Website. The original project is available at https://www.figma.com/design/3edgsX1cOMwDjzoa5HMedz/Agency-Multipage-Website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  